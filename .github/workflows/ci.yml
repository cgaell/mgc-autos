name: MGC CI/CD Pipeline

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]

jobs:
  test-and-verify:
    name: Pruebas Unitarias y Cobertura (Jest)
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: milleniumglobal
          MYSQL_DATABASE: mgc_seguros
          MYSQL_USER: mgc_admin
          MYSQL_PASSWORD: milleniumglobal
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping -pmilleniumglobal" --health-interval=10s --health-timeout=5s --health-retries=5

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Instalar dependencias
        run: npm ci

      - name: Inicializar tablas y esquema de MySQL
        run: node src/database/seed.js
        env:
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_NAME: mgc_seguros
          DB_USER: mgc_admin
          DB_PASSWORD: milleniumglobal

      - name: Ejecutar pruebas con cobertura (>= 80%)
        run: npx jest --coverage --runInBand --forceExit
        env:
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_NAME: mgc_seguros
          DB_USER: mgc_admin
          DB_PASSWORD: milleniumglobal
          JWT_SECRET: llave_secreta_mgc

  deploy-staging:
    name: Despliegue en Entorno de Pruebas
    needs: test-and-verify
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'

    steps:
      - name: Simulación de Entrega Continua
        run: echo "Pruebas pasadas en contenedor MySQL. Despliegue completado."